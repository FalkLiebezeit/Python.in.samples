import numpy as np

# --- Create a 1D numpy array with even numbers ---
array = np.array([2, 4, 6, 8, 10])

# --- Output the array ---
print("Numpy array:", array)

# --- Output the type of the array ---
print("Type of array:", type(array))