# Example: Get the length of a list (array) in Python

lst = [23, 56, 67]  # Declare a list with three elements
val = len(lst)      # Get the length of the list using len()

# Output the length with a clear message
print("The length of the array is", val)